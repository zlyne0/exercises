'use strict';

describe('Controller: one', function () {

  beforeEach(module('productParametersApp'));

  it('should attach a list of awesomeThings to the scope', function () {
    expect(1).toBe(1);
  });
});
